/* ========================================================================
   云同步配置（可选）
   把 Firebase 项目的网页配置粘贴到下面即可开启「安卓 ↔ iPad」自动同步。
   留空则 app 只在本地保存，功能完全正常，只是不跨设备同步。
   详细步骤见「托管与云同步指南.md」。
   注意：这几个值不是密码，可以公开（安全由 Firestore 规则控制）。
   ======================================================================== */
window.FIREBASE_CONFIG = {
  apiKey: "",
  authDomain: "",
  projectId: "",
  appId: ""
};
